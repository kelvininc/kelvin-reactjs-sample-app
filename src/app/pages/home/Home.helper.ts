/**
 * place to have helper funtions we should extract as much as possible the component logic out
 * and put it as a helper function so it can be tested
*/

function parseResponse() {

}

export default {
    parseResponse
}